{
  "buildCommand": "pnpm build:lib && pnpm build:docs",
  "outputDirectory": "apps/docs-site/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "rootDirectory": "apps/docs-site"
}

