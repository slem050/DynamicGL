{
  "name": "dynamicgl",
  "version": "0.1.0",
  "private": true,
  "description": "GPU-accelerated charting framework for real-time data",
  "scripts": {
    "dev": "nx run docs-site:serve",
    "build": "nx run-many --target=build --all",
    "build:lib": "nx run-many --target=build --projects=core,charts,theme,react",
    "build:docs": "nx run docs-site:build",
    "typecheck": "nx run-many --target=typecheck --all",
    "lint": "nx run-many --target=lint --all",
    "graph": "nx graph",
    "publish:packages": "nx run-many --target=build --projects=core,charts,theme,react && pnpm --filter @dynamicgl/core publish --access public --no-git-checks && pnpm --filter @dynamicgl/charts publish --access public --no-git-checks && pnpm --filter @dynamicgl/theme publish --access public --no-git-checks && pnpm --filter @dynamicgl/react publish --access public --no-git-checks"
  },
  "devDependencies": {
    "@nx/next": "^22.3.3",
    "@nx/vite": "^22.3.3",
    "@nx/workspace": "^22.3.3",
    "@types/node": "^20.10.0",
    "nx": "^22.3.3",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "vite-plugin-dts": "^3.6.4"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
